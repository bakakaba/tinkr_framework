syntax = "proto3";

package hello;

// A minimal greeter service used to demonstrate multiplexed gRPC + HTTP.
service Greeter {
  // Returns a greeting for the given name.
  rpc SayHello (HelloRequest) returns (HelloReply);
}

message HelloRequest {
  string name = 1;
}

message HelloReply {
  string message = 1;
}
